# CineFile_it

CineFile_it is an open-source, AI-assisted video annotation and cataloging studio. It simplifies the process of watching films and extracting rich, localized metadata directly into a structured CSV format. By conversing with an AI assistant that lives alongside the video, annotators can talk through their analysis and have the system automatically draft fields like Description, Historic Context, and Aesthetic Commentary.

**Background:** This tool was originally conceived and developed for the **Agente Fílmico** project as a bespoke annotation tool tailored to the needs of independent audiovisual archives. While its roots are in addressing these specific archival needs, the platform is designed to be easily expanded and adapted to other archives and collections. For more information on the origins of the project, visit [Third Eye Files - Agente Fílmico](https://thirdeyefiles.net/agente-filmico-at-eye/) and [Agente Fílmico (MAFI)](https://agente.mafi.tv/).

**Platform Context:** CineFile_it was built primarily to run within the Google AI Studio environment, utilizing its rapid prototyping capabilities. However, it has been developed with a conscious awareness that it can be easily hosted, customized, and adapted to run on other platforms, servers, or local environments.

## How it works

1. **Spreadsheet Ingestion:** The app reads a public Google Sheet (or a CSV file) containing the backlog of films that need to be annotated.
2. **AI Co-pilot:** Annotators use a built-in chat interface powered by Large Language Models (LLMs) to talk through their analysis while watching the video.
3. **Drafting & Approvals:** The AI drafts the text directly into the metadata fields. Annotators revise the copy and manually hit "Approve". 
4. **Export:** Once work is done, annotators export their localized progress as a `.csv` delivery file, which can be sent back to project administrators.

## Documentation Reference files

Please refer to these Markdown files in the root folder, which define how the project operates:

- [**Annotator Manual (`USAGE.md`)**](USAGE.md): Detailed instructions on how to use the app, manage saves/exports, and interact with the AI. (This file is also read by the AI to answer user questions regarding the UI!)
- [**Spreadsheet Setup (`SPREADSHEET_CONFIGURATION.md`)**](SPREADSHEET_CONFIGURATION.md): How to link the app to your own Google Sheet using the provided template.
- [**AI Configuration (`AI_CONFIGURATION.md`)**](AI_CONFIGURATION.md): How to configure API keys, models, and priority cascading between OpenRouter, Gemini, Anthropic, and Mistral.
- [**Annotation Guidelines (`ANNOTATION_GUIDELINES.md`)**](ANNOTATION_GUIDELINES.md): The prompt guardrails that enforce the tone and formatting of the metadata generated by the AI assistant.
- [**Future Implementations (`FUTURE_IMPLEMENTATIONS.md`)**](FUTURE_IMPLEMENTATIONS.md): Upcoming roadmap and feature ideas.

## Installation & Running Locally

1. Clone this repository to your local machine.
2. Make sure you have Node installed (v18+ recommended).
3. Install the dependencies:
   ```bash
   npm install
   ```
4. Follow the setup instructions in `AI_CONFIGURATION.md` to add your API keys (via `.env` or otherwise).
5. Start the development server:
   ```bash
   npm run dev
   ```
6. Open your browser and navigate to `http://localhost:3000` (or whatever URL your terminal outputs).
